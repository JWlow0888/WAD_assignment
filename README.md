If you want to test run the code downlaod the gamegear and gamegear_exchange.sql and paste gamegear in your WWW in WAMP64 directory, and import 
gamegear_exchange.sql into localhostphpadmin.

If you want to add in your code, either edit the code inside the directory in github or download the current version, then add in your code and then post the code in here 
so we can view the changes made.
