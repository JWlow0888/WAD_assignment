If you want to test run the code downlaod the gamegear and gamegear_exchange.sql and paste gamegear in your WWW in WAMP64 directory, and import 
gamegear_exchange.sql into localhostphpadmin.

If you want to add in your code, edit the code inside the directory in github or download the current version, then add in your code here.
But make sure you don't change anyone's code part by accident. So make sure you have the copy original code and the new version of the code. 

Updata 25-8
Please delete your old databases and import the new gamegear_sql i have integrated the databases please check your part is ok

Update 27-8
Please make sure your new code uses the new database fields and don't make another sql database follow the gamegear_exchange.sql.
If you update your code and you want to make a new table in the database do so in gamegear_exchange.sql and delete the old database and import the new one in phpMyAdmin. This is because we need to know you added a new table or not.

Update 3-9
Added Responsive Design Implementation for css for marking rebrics
changes are made in listings index and search_listings

Update 5-9 
deleted some dead code purchase.php in gamegear folder not the one in purchase folder
